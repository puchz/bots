{
  "name": "con-lo-ojo",
  "version": "0.1.3",
  "description": "Bot de telegram en Docker",
  "author": "con lo ojo",
  "license": "MIT",
  "scripts": {
    "compile": "npx tsc",
    "build": "npm run clear && npm install && npm run compile",
    "clear": "rm -rf ./node_modules && rm -rf ./dist",
    "start": "node ./dist/app.js"
  },
  "dependencies": {
    "node-fetch": "^2.6.6",
    "node-telegram-bot-api": "^0.53.0",
    "weather-js": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.17",
    "@types/node-telegram-bot-api": "^0.53.0",
    "typescript": "^4.9.4"
  }
}
